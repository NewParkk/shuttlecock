<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fp.shuttlecock.announcement.AnnouncementMapper">
	<select id="getboardList" resultType="AnnouncementDTO">
		SELECT *
		FROM announcement
	</select>
	
	<select id="getpostDetailbypostId" resultType="AnnouncementDTO">
		SELECT *
		FROM announcement
		WHERE announcementNo = #{postId}
	</select>
	
	<insert id="insertPost" parameterType="AnnouncementDTO">
		INSERT INTO announcement
		(title, content, user_userId)
		VALUES
		(#{title}, #{content}, #{userId})
	</insert>
	
	<update id="updatePost" parameterType="AnnouncementDTO">
		UPDATE announcement
		SET title = #{title},
			content = #{content},
			user_userId = #{userId}
		WHERE announcementNo = #{postId}
	</update>
	
	<delete id="deletePostbypostId" parameterType="int">
		DELETE
		FROM announcement
		WHERE announcementNo = #{postId}
	</delete>
	
	<select id="getPostbyPage" parameterType="map" resultType="AnnouncementDTO">
		SELECT * FROM announcement 
		ORDER BY announcementNo DESC LIMIT #{offset}, #{postperPage}
	</select>
	
	<select id="getSearchedPostPage" parameterType="map" resultType="AnnouncementDTO">
		SELECT * 
		FROM announcement
        WHERE title LIKE CONCAT('%', #{query}, '%') 
        	  OR content LIKE CONCAT('%', #{query}, '%')
        ORDER BY announcementNo DESC LIMIT #{offset}, #{postperPage}
	</select>
	
	<select id="searchedPostCount" resultType="int">
		SELECT count(*)
		FROM announcement
		WHERE title LIKE CONCAT('%', #{query}, '%') 
        	  OR content LIKE CONCAT('%', #{query}, '%')
	</select>
	
	<select id="searchedPostList" parameterType="String" resultType="AnnouncementDTO">
		SELECT *
		FROM announcement
		WHERE title LIKE CONCAT('%', #{query}, '%') 
			  OR content LIKE CONCAT('%', #{query}, '%')
	</select>
	
	<select id="countPosts" resultType="int">
		SELECT count(*)
		FROM announcement
	</select>
	
</mapper>